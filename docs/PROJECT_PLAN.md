# follow-anything — Project Plan & Contract

> **Single source of truth** for what `follow-anything` is, what it will and won't do, and how "done" is defined per stage. It is **version-controlled on purpose**: any change to scope or plan must be an explicit edit to this file, committed with a clear message and logged in §7. That is the anti-drift mechanism — **if a change isn't reflected here (and in git history), it is not an agreed change.**

## 1. North-star goal
Build a **promptable, track-anything video system**: point at any object (a player, a car, a person, an animal — even a category never trained on) and follow it through a clip; detect and track *everything* on screen with persistent identities; and, as the flagship, lift the scene into a **3D bird's-eye reconstruction**. **Football is the proving ground / showcase** (dense, fast, near-identical targets, broadcast camera cuts = tracking on hard mode); the pipeline is domain-agnostic.

**Portfolio thesis:** demonstrate, end-to-end and *rigorously*, the ability to build a modern CV tracking system — the standout artifact being a **real-time, on-device promptable tracker** characterized on an honest accuracy-vs-latency Pareto (engineering + rigorous characterization, not new-SOTA research).

## 2. Scope
**In scope:** promptable single/multi-object tracking (SAM 2) + full-scene detect+track (YOLO + BoT-SORT); football showcase on SoccerNet SN-GSR-2025; the 6-rung ladder (§4); rigorous evaluation; efficiency/real-time/on-device work (distillation, quantization, export); a public repo + shareable demos.

**Out of scope (unless explicitly added here):** multi-camera stadium rigs; production/live-broadcast infrastructure; a commercial guaranteed-uptime system; non-video modalities; beating foundation-model SOTA / novel-architecture research; fully-automated 90-minute live production.

**Extending scope is allowed but must be intentional:** edit §2 + §4, commit, and log it in §7. Codex flags anything in the repo that isn't traceable to this section.

## 3. Invariants (non-negotiable — checked every review)
1. **Leak-free evaluation** — splits are game/match-disjoint; no clip from a test match appears in training. Frame-level random splits are forbidden.
2. **Metrics validated before use** — every metric is unit-tested to known values before being trusted on real data.
3. **Honest baselines** — no reported number without a sanity check; a too-good/too-bad result is investigated before it is published.
4. **Reproducibility** — every reported number reproduces from a committed script + config.
5. **No silent scope drift** — all work maps to a rung in §4; anything else is flagged.

## 4. The rungs (definition of done)
A rung is "done" only when its **gate** is met *and verified* (by us + a Codex adversarial pass).

| # | Rung | Gate (measurable definition of done) | Status |
|---|---|---|---|
| 1 | **Baseline + measurement foundation** | Leak-free game-disjoint split; HOTA/MOTA/IDF1 via TrackEval, unit-tested to *exact* values; a reproducible (provenance-stamped) zero-shot baseline confirmed on an untouched final set | ✅ **done** — dev 0.481 / **untouched-final 0.492** (§7) |
| 2 | **Promptable "click, follow"** | A working spotlight video from a single click/box prompt on real football (SAM 2 mask propagation) | ✅ done (demo) |
| 3 | **Robust** | Beat the *matched-set* baseline on the **held-out** final set at **full precision** — HOTA **> `final.metrics.HOTA` = 0.49209979323992903** in [`results/rung1_baseline.json`](../results/rung1_baseline.json), **not** the rounded `0.492` (a 0.49205 candidate is *worse* yet clears a rounded bar) — on `valid` game 2 (identical eval) **and** dev HOTA **> `dev.metrics.HOTA` = 0.4814830134807477** on game 4; the gate check reads those JSON fields directly. Write up *what* moved it; occlusion/re-ID/ball measured (AssA ↑, IDSW ↓) | ✅ **done, provably leave-one-game-out** — dev **0.634** / final **0.614** (§7); AssA 0.40→0.52, IDSW 1869→868; ball + occlusion + teams measured. *(Game 2 is a **reused** held-out benchmark, not a pristine one-shot — §7 2026-07-22.)* ⏳ **re-certification pending** — round-5 cache defence, §7 2026-07-23 |
| 4 | **Permanence** | A benchmark of ID consistency across shot cuts/viewpoints + a method that measurably improves it vs. Rung 3 | ⬜ |
| 5 | **Real-time / on-device** (novelty focus) | An **accuracy (HOTA/IoU) vs latency (fps) Pareto** on our data + target hardware, with an exported runtime (CoreML/ONNX/TensorRT) and ≥1 point improving on off-the-shelf efficient variants (EdgeSAM/EdgeTAM/EfficientTAM lineage) | ⬜ |
| 6 | **3D tactical replay** (flagship) | Pitch homography → top-down minimap → depth-lifted 3D; a demo from a *single* broadcast camera | ⬜ |

Detailed per-rung deliverables & rationale live in each `handoff/rung-N/handoff.md`.

## 5. Current status
- **Rungs 1–3 done** (Rung 1 hardened + confirmed after an adversarial review; Rung 3 fine-tuned detector beats the held-out final baseline **0.492 → 0.614**, provably leave-one-game-out after four adversarial rounds — **round 5 re-certification pending** (§7 2026-07-23). Public repo: https://github.com/gimocimo/follow-anything
- **Baseline to beat: HOTA 0.481 (dev, game 4) / 0.492 (untouched-final, `valid` game 2)** — bbox-HOTA, all object categories; the two agree, so it isn't contamination-inflated. *(These are rounded for display; the Rung-3 gate compares against the full-precision values in `results/rung1_baseline.json`.)* Official metric later: **GS-HOTA** (pitch coordinates).
- Caveats: internal package still named `pitchvision`; local dev on Apple **MPS** (~1 fps SAM 2, a Mac/hardware limit as much as a model one); disk tight; GSR-`train` has only 3 games → **leave-one-game-out** split. **Hard data ceiling:** labelled `train`+`valid` together contain only **6 games** {2,3,4,5,6,9}; with 4 reserved as the dev benchmark and 2 as the final, {3,5,6,9} is *all* the match diversity available — the detector is diversity-limited, not compute-limited (§7 2026-07-22).

## 6. Review protocol (Codex adversarial passes)
After each rung is completed:
1. Write `handoff/rung-N/handoff.md` (from `handoff/TEMPLATE-handoff.md`): what was built, results, decisions, known issues, how to reproduce.
2. Run Codex with `handoff/rung-N/codex-review-prompt.md`. Codex reads **this plan + the handoff + the repo**, then runs an **adversarial audit**: (a) verify the rung's gate is *genuinely* met; (b) try to break the claims/numbers; (c) check the §3 invariants; (d) check repo state vs this plan (drift); (e) prioritized fixes + enhancements.
3. Paste Codex's output into `handoff/rung-N/codex-report.md`; triage; fix; optionally re-review.
4. **Do not advance** to the next rung until the current gate is verified and blocking findings are resolved.

`handoff/` is **gitignored** (local working scaffolding, not the public deliverable). This plan is **tracked** (the contract).

## 7. Decision log (append-only — every intentional scope/plan change)
- **2026-07-13** — Plan laid down. Data source pivoted SoccerNet-Tracking → **SN-GSR-2025** (KAUST share returned 401/dead). Repo renamed `pitch-vision` → **follow-anything** (general scope; football = showcase). Rungs 1–2 complete. Codex adversarial-review process adopted.
- **2026-07-13 (later)** — **Rung-1 Codex review returned NO.** Adopted a hardening pass: committed a fail-closed **leave-one-game-out** split (fixes the empty-test-set default); strengthened the metric self-test to **exact** values (+ IoU-sensitivity case) under pytest/CI; added a **provenance manifest** to the baseline eval; require `info.game_id` for gate splits; **category policy = include category-7 "other"** (baseline stays 0.481, per-class diagnostics added); relabeled **game 4 as a dev benchmark** and reserved the official **`valid`** split as the untouched final-confirmation set (downloading); removed dead SoccerNet-Tracking code (`scripts/01`(old)/`03`/`04`, `data/soccernet.py`) and fixed the public data path.
- **2026-07-13 (later still)** — **Rung-1 hardening complete + confirmed.** P1s closed (commit 26d3f24): vendored `botsort.yaml`, strict fail-closed conversion + integrality guards, tracker-persistence regression test in CI, tabulate. Downloaded `valid` (11 GB, via stall-detecting curl after the Python downloaders kept stalling) and ran the **untouched final baseline**: **HOTA 0.492** (`valid` game 2, LOO) — matches the dev 0.481, so the baseline is *not* evaluation-selection-inflated. **Rung 1 → done**; ready for Codex re-review, then Rung 3.
- **2026-07-14** — **Rung-1 *second* Codex re-review returned NO — several first-pass fixes were only cosmetic.** The vendored `botsort.yaml` was never wired into the baseline path (`run_image_folder` passed the raw name, so `_resolve_tracker` only touched the unused mp4 path); the persistence test guarded a static golden file, not production; the provenance manifest was git-ignored; the Rung-3 gate was still stale at 0.481. **Fixed + committed (`d565ce5`, `e257804`):** `run_image_folder` now calls `_resolve_tracker(tracker)` and the runtime manifest records the resolved path + sha256; added a production-path test asserting the real streaming `model.track` call; committed portable `results/rung1_baseline.json`; updated the §4 gate to require beating the matched-set 0.492. **Lesson recorded: verify fixes end-to-end, not by reading the diff.**
- **2026-07-14 (later)** — **Rung-1 *third* Codex re-review returned NO but "close".** F1–F10 independently reproduced closed (final HOTA `0.49209979323992903` reproduced to 2.6e-7; zero cross-source leakage; plain `pytest` = 10 passed). Two must-fixes + this log gap remained: **(1)** the production-path test still accepted a *plausible* per-frame reset — it created only one frame, so "exactly one call" was vacuous, and it never asserted `source ==` the folder; **(2)** the §4 Rung-3 gate rounded to `> 0.492`, so a **0.49205 regression would literally pass**; **(3)** the second-hardening entry above was missing from this log (append-only anti-drift breached). **Fixed:** the production test now uses ≥2 frames, asserts `source ==` the folder (exact resolved-path tracker equality), and **ships two negative controls** (a per-frame driver and a single-frame source are both rejected). A **mutation check** (reverting `run_image_folder` to per-frame makes the production test *fail*; restoring it passes) confirmed the guard is a genuine discriminator — reproduced independently by Codex — but is a verification step, not a committed test; the §4 gate now cites the **full-precision** `final.metrics.HOTA` / `dev.metrics.HOTA` fields in `results/rung1_baseline.json`; this entry is appended; and a Low was fixed — the README final-baseline command now passes `--out-dir outputs/gsr_baseline_final` so it no longer overwrites the dev run. **Deferred (Low, non-blocking → Rung 3):** portable provenance does not yet pin the HF dataset revision / label hashes (data-identity), and per-class tracking metrics (per-class HOTA) — to fold into Rung 3's per-class occlusion/ball analysis.
- **2026-07-15** — **Rung 3 → done (gate MET).** Approach, all tuned on DEV only: **3a** BoT-SORT association sweep → `new_track_thresh=0.40` winner (`configs/trackers/botsort_newtrk040.yaml`, +0.017); **3b** detector sweep → detector strength dominates ~4:1 over tracker knobs, `imgsz 1280 > 1536`, zero-shot yolo11x already hit dev 0.564; **3c** fine-tuned **yolo11m** on GSR (`scripts/09_gsr_to_yolo.py` + `10_train_detector.py`), trained **leave-one-game-out on games {6,9}** — disjoint from BOTH eval games, so one model scores both and dev/final are matched. **Result: dev HOTA 0.627, sealed-final (valid game 2, one-shot) HOTA 0.609** — beats the full-precision gates (0.4815 / 0.4921); +24% on the sealed match, DetA 0.60→0.71, **AssA 0.40→0.52, IDSW 1869→910**; dev≈final so it generalises. Fine-tuned m beats zero-shot x (0.564) → domain adaptation > model size. Provenance: `results/rung3_baseline.json` (+ runtime manifests `outputs/gsr_ft_{dev,final}/`). Ran on a rented **RunPod RTX 4090** (the RTX 5090 / Blackwell was too new — hung/NaN under this torch build; not a power issue). Infra also added: `scripts/08_rung3_sweep.py`, `06 --classes`, `10 --workers/--amp`. **Still open (for the Codex review / writeup): per-class HOTA (esp. ball) + explicit occlusion breakdown** — the AssA↑/IDSW↓ shape is shown, but the per-class split is not yet computed. Next: Codex adversarial review of Rung 3, then Rung 4.
- **2026-07-15 (later)** — **Per-class analysis added** (`scripts/11_perclass_eval.py`), closing the open item above. DEV game 4: **person HOTA 0.64** but **ball 0.12** (DetA 0.15 — detected ~15% of the time). The 0.62 headline is player-driven; the ball (tiny/fast/single-instance, ~6% of training boxes) is barely tracked. `all`-group reproduces the headline to ~0.003 (MPS vs CUDA). Recorded honestly in README + `results/rung3_baseline.json` (`per_class_dev`). **Ball tracking = the explicit next target** (higher-res/crop inference, ball oversampling, ball-specific tracker params — a candidate mini-rung / Rung-4 input). Also noted for the auditor: `new_track_thresh=0.40` was tuned on the dev game, so the **dev** number is mildly optimistic; the **sealed-final 0.609 (game 2) is the clean, untuned number** the gate rests on.
- **2026-07-18** — **Rung-3 Codex review returned NO; fixed + RETRAINED for a provable checkpoint.** The review confirmed the numbers reproduce and beat both baselines, but raised a **Blocker** (the GSR→YOLO exporter could silently include eval-game frames, and the original pod checkpoint's training dir was gone → leak-free *unprovable*), **High** (training provenance incomplete; occlusion never measured), + mediums (docs/cache/wording). **Fixes:** hardened `scripts/09` (fail-closed on a non-empty `--out` + `export_manifest.json` fingerprint; reproduced *and* blocked the contamination attack); `scripts/10 --resume` + `train_manifest.json` linking the checkpoint to the export fingerprint; new `scripts/12_occlusion_eval.py` (ID fragmentation + gap-recovery); sweep cache-key, `strict=True`, wording. Then **retrained on RunPod** with the hardened pipeline. **New PROVABLE result** — chain `export_sha256 1dcf475b… → train_manifest → best.pt d3115ca5… → metrics` all cross-checked, games **`{6,9}` only**: **dev HOTA 0.625, sealed-final 0.596** (beats 0.4815 / 0.4921; supersedes the pre-retrain 0.627/0.609 — within training variance). **Occlusion (dev persons):** mean **2.79 tracker-IDs/player, 26% cleanly tracked, gap-recovery ~4%** → the Rung-4 target. Proof committed to `results/rung3_provenance/`; `results/rung3_baseline.json` regenerated *from* the runtime manifests. Ledger note (one-shot): the sealed game-2 eval was run once under the pre-registered §4 gate. Remaining before Rung 4: rewrite the handoff doc to the actual method, then Codex re-review.
- **2026-07-18 (later)** — **Rung-3 re-review: NO. Occlusion claim WITHDRAWN.** Codex re-verified the gate (fresh dev **0.62484** / final **0.59573**, +0.1434 / +0.1036 over the full-precision gates; model + tracker hashes match; splits game-disjoint; `pytest` 12 passed, Rung 1 intact) and confirmed the hardened exporter genuinely blocks the contamination attack. Three assurances still failed: **(1) not provably leak-free** — the trainer never verifies the dataset it *actually consumes*; a frame injected **after** export is loaded by ultralytics while `train_manifest` still describes the clean export. **(2) The occlusion metric is construct-invalid** — 25/27 "visibility gaps" were **frame-edge exits**, and 26/27 apparent failures were **detection** failures (conditional same-ID retention was **1/1**). The published "≈4% occlusion recovery" is therefore **WITHDRAWN** from the README; a genuine on-screen benchmark (interior mutual-overlap events, endpoint coverage reported separately, provenance-stamped) will replace it. **(3)** Sweep cache compares paths not artifact hashes; `10`'s resume path parses `args.yaml` with `json.loads` → silently nulls `dataset_export`; `train_manifest` lacks the output-checkpoint SHA; `rung3_baseline.json` has no committed generator; and the Rung-3 hardening has **no tests**. Minor: `09`'s docstring still cites a {4,6,9} final detector; "players average 2.8 IDs" is really *per-clip person track-instances incl. GK/ref*; detector-vs-tracker gain is **3.4×** matched (not "~4:1" — corrected in README). **Plan:** per-file image+label hashes in the export inventory → trainer pre/post-flight verification that aborts on any drift → record `best.pt` SHA in the same manifest → adversarial regression tests for each attack → **one final retrain** (bundled with the ball-oversampling experiment so the GPU run does double duty). Until then Rung 3 is **gate-met but not provenance-closed**, and Rung 4 does not start.
- **2026-07-18 (later still)** — **Rebuilt occlusion benchmark validated — and the finding FLIPS.** `scripts/12` now measures genuine on-screen occlusion (a player overlapped by *another player* at IoU ≥ 0.3, entered **and** exited while interior to the frame) and reports detection coverage separately from identity retention. On 6 dev clips: **369 real episodes** (vs the withdrawn metric's 27 "gaps"), **endpoint coverage 310/369 = 84%**, **conditional same-ID retention 286/310 = 92.3%** (24 switches). The edge/interior split independently reproduces Codex's decomposition (interior **1** vs frame-edge **14**) — confirming the withdrawn statistic was measuring frame-edge exits. **Conclusion: association *through* occlusion is strong (~92%); the real weakness is DETECTION CONTINUITY** — 16% of episodes lose the player entirely, and fragmentation stays high (2.87 tracker-IDs per person track-instance, only 19% single-ID). This re-frames **Rung 4**: permanence is less "re-ID after occlusion" and more "don't drop the detection in the first place, plus long-horizon re-acquisition". Final figures to be recomputed on the retrained checkpoint.
- **2026-07-18 (final)** — **Rung 3 retrained on a VERIFIED dataset — the blocker is closed.** `scripts/10` ran the preflight (`dataset verified: 5850 files, games ['6','9'], export_sha256 f313f3a7…`) before a single gradient step and again after; `train_manifest.json` records `dataset_verified_pre/post.ok = true`, `provably_leave_one_game_out = true`, and `output_best_sha256` matching the checkpoint. Chain committed to `results/rung3_provenance/` (incl. the 5,850-file `export_inventory.json` with per-file image+label hashes). **Result (unchanged, as expected): dev HOTA 0.6248, sealed-final 0.5964** — gate MET at full precision. **Notable:** the retrain produced **bit-identical weights** to the previous run — all 649 tensors, max abs diff 0.000, differing only in the checkpoint's embedded timestamp/version. Training is therefore deterministic, and since identical data + recipe ⇒ identical weights, this *retroactively corroborates* that the earlier (unverified) checkpoint also trained on clean {6,9} data. **Occlusion on the full dev set:** 1,510 on-screen episodes, endpoint coverage **84.9%**, conditional same-ID retention **91.0%**; fragmentation 2.79 IDs/track-instance (26% single-ID) → Rung 4 targets **detection continuity**, not re-ID. Next: Codex re-review (round 3), demo clips, then Rung 4.
- **2026-07-19** — **Rung-3 re-review round 3: NO. Three self-inflicted errors, all legitimate.** Codex confirmed the numbers independently (fresh dev **0.6248407**, sealed final **0.5957285**, beating the gates by +0.1434 / +0.1036; checkpoint SHA `3c45e3d4` used by both; Rung 1 intact; 19 tests green) and confirmed my four documented attacks are blocked. But it broke production three new ways. **(1) The `data.yaml` attack — a complete bypass:** `export_verify` authenticates the *directory*, while ultralytics is handed an unauthenticated YAML that can point `train:` anywhere. Codex pointed it at real game-4 frames; training completed and the manifest still recorded `provably_leave_one_game_out: true`. Nested subdirectories and non-`.jpg/.png` formats also bypass, because the scan is non-recursive and format-limited while ultralytics' loader is neither. **(2) The results generator fails OPEN:** missing hashes compare `None == None` → links reported true; changing `observed_games` to `{4,6,9}` still yields `provably_leave_one_game_out: true` because the generator trusts the stored flag; Rung-1 gates are hardcoded rather than read from `results/rung1_baseline.json`. **(3) The occlusion metric was overinterpreted and the conclusion published:** the 91% is *endpoint* ID equality on episodes ≤30 frames; the 122 longer episodes (silently excluded) retain only **68.3%**; ~93 "retained" cases break identity mid-episode and recover; only **67%** are detected continuously; and the 1,510 unit double-counts mirrored pairs, so it is a *per-person box-overlap episode* count, not 1,510 distinct occlusions. It also has **no unit test**, violating §3's "metrics validated before use". README claim corrected/flagged; the Rung-4 redirection it motivated is **withdrawn pending** a corrected metric. Also open: cache still keys on split *path+count* (different 18 sequences at the same path reuse cached results); `--resume` verifies the passed `--data` while ultralytics restores the checkpoint-embedded dataset; postflight only prints instead of failing; handoff doc stale. **Plan:** authenticate what the loader *actually resolves* (compare ultralytics' own `train_loader.dataset.im_files` against the inventory via an `on_train_start` callback — unfakeable by YAML redirection, nesting or format tricks), hash+retain `data.yaml`, make postflight and resume fail closed, make the generator and cache fail closed, duration-stratify + rename + unit-test the occlusion benchmark, then bind the next training run. **Rung 3 remains gate-met but not provenance-closed; Rung 4 does not start on the occlusion claim.**
- **2026-07-21** — **Scaled the training set and the class scheme; result held, and the data ceiling became visible.** Rebuilt the export as **4 classes** (player / goalkeeper / referee / ball) over **every frame of games {5,6,9}** (43,500 imgs) with **game 3 held out entirely as cross-match validation** (1,575 imgs @ stride 10) — so early stopping measures generalisation to an *unseen match* rather than memorisation. Trained **yolo11l @ 1536** on a rented A100 80GB. **Finding: the model plateaus at epoch 1** — ten further epochs never beat it (early-stop at 11, ~7.4 h) — while the tracking gate still improved (dev HOTA 0.6335). Diagnosis: **data-diversity-limited, not compute-limited.** SN-GSR's labelled `train`+`valid` holds only **6 games**; with 4 (dev) and 2 (final) reserved, {3,5,6,9} is *all* the diversity that exists, so bigger models / more epochs / more frames cannot fix cross-match generalisation. A **1920-resolution probe** (re-eval of the 1536-trained checkpoint) made every metric slightly *worse* (all-HOTA 0.6335→0.6280; ball DetA +0.009 but AssA −0.014, IDSW 922→984) ⇒ **resolution is not the ball's bottleneck**; the ball needs a dedicated sub-project, not a global knob. Also added a **stop-file guard** (`--stop-file`, `on_fit_epoch_end`) so a run stopped to cap GPU spend still finishes validation, saves `best.pt` and writes a valid manifest — a budget-killed run previously destroyed the proof.
- **2026-07-22** — **Rung-3 re-review round 4: NO → fixed, retrained, and now genuinely certified.** Codex confirmed the checkpoint was clean (inventory holds zero game-2/4 frames, `best.pt` hash matches both eval manifests, gains reproduce, 47 tests green) but broke the *proof machinery* three ways. **(1) The loader guard was split-blind and label-blind** — it compared the **union** of resolved train+val image paths, so a `data.yaml` with `train: [images/train, images/val]` trained on the held-out val while every check passed; and image **symlinks** under an in-root alias dir resolved to manifested paths while ultralytics read attacker-controlled **labels**. **(2) The results generator certified contradictory evidence** — links tested shallow `.ok` booleans, so pre/post records with unrelated hashes, wrong games and contradictory counts all passed, as did a guard with `ok:true` and no counts, and a missing `data_yaml_sha256`; `build()` **hardcoded** final game 2 and copied the tracker from dev, so a final manifest describing a different game/tracker/imgsz stayed green; and the malformed-hash test mutated only **one** side, so dropping the 64-hex check kept tests passing. **(3) The committed result was not regenerable** — dev/final `baseline_metrics.json` lived only under gitignored `outputs/`. Plus a **High**: game 2 has now been scored on ~4 checkpoints (0.609, 0.596, 0.5957, 0.6139), so "sealed one-shot" was no longer honest. **Fixes (`5135b22`, `0256a1f`, `19686a2`):** `export_verify.authenticate_loader` verifies train and val loaders **separately** against their inventory subsets, compares paths **lexically** (no symlink resolution, so alias dirs are caught by name), rejects overlap, and **hashes every label the loader will consume**; `check_data_yaml` now demands exactly the canonical `images/train` / `images/val`; both reproduced attacks are committed tests. `leak_free_links` cross-binds pre/post export-sha + games + counts, loader split + labels + count, `data_yaml_sha256`, and **derives** each eval game from its own manifest's split meta against a **non-overridable** canonical policy (train {3,5,6,9}, dev 4, final 2), plus identical tracker/imgsz/classes across dev and final; the generator reads **committed** evidence in `results/rung3_provenance/{dev,final}` so a clean clone regenerates the file. **Retrained under the hardened guard** (`l_1536_v2`): the manifest records `train_matches_manifest` / `val_matches_manifest` / `labels_authenticated` over 43,500 train + 1,575 val files, and all **14** links pass ⇒ `provably_leave_one_game_out: true`. **Result: dev HOTA 0.6340 / final 0.6139** — gates 0.4815 / 0.4921 **MET** (+0.1526 / +0.1218); chain `export_sha256 98cfbc8f… → train_manifest → best.pt 57d95785… → both eval manifests`. **Corrections:** the recipe is "every frame of **{5,6,9}**, game 3 = cross-match val" (the earlier "every frame of {3,5,6,9}" was wrong); **game 2 is relabelled a reused, held-out, game-disjoint benchmark, NOT a pristine one-shot** — with only 6 labelled games no unseen match remains to reserve, so the reuse is disclosed instead of overclaimed; README/§4/§5 updated off the superseded yolo11m `{6,9}` numbers. Also learned: **bit-determinism holds within an ultralytics version but not across** (8.4.102→8.4.103 changed `best.pt` from `d3ad0aa7…` to `57d95785…` while leaving metrics unchanged to 3 decimals) — so the earlier "identical data + recipe ⇒ identical weights" claim needs the version pinned. Ball remains weak (HOTA **0.158**) → its own sub-project; team assignment **85.5%**; occlusion endpoint coverage **85.2%** / conditional retention **91.0%** / fragmentation **3.30** IDs per track-instance. **Lesson: "provable" must survive an adversary *reconstructing the attack end-to-end*, not merely read correct on inspection.**
- **2026-07-23** — **Rung-3 re-review round 5: NO — two new fail-open paths, both real.** The round-4 fixes held (Codex re-ran the train-list and a *real* symlink-alias attack: both abort; reverting the guard fails four tests; clean-clone regeneration byte-identical; game-2 relabelling accepted as sufficient). Two new blockers. **(1) Poisoned ultralytics label cache.** Ultralytics loads `labels/{train,val}.cache` *before* `on_train_start`, so the guard authenticated the on-disk `.txt` while the model consumed cached annotations; the export scan also ignored `.cache` because it filtered on `.txt`. Codex reproduced it end-to-end on the certified runtime (8.4.103): canonical class-0 labels on disk, class-3 in the cache — production reported preflight verified, splits matched, `labels_authenticated: true`, postflight verified, `provably_leave_one_game_out: true`, while final validation reported **ball**. **(2) The generator was still under-bound** — it checked records against *each other*, not against ground truth: counts of `1` against a 45,075-frame export, `ok:true` beside `fingerprint_match:false`/`modified:['x']`, a non-empty loader `problems` list, an embedded `observed_games:['4']`, a 1-sequence final evaluation, `conf` 0.99, and *both* evaluations moved to imgsz 640 all certified green (an equality check between dev and final cannot see the last one). **Fixes:** `purge_label_caches` deletes caches before the loader is built (and again before postflight), forcing ultralytics to parse the authenticated `.txt`; `labels_in_memory_mismatched` authenticates `dataset.labels` — the *parsed* annotations — against those files, failing closed when unavailable; `verify_export`'s tree scan is now extension-agnostic, so a `.cache` counts as drift; the generator anchors on the **committed inventory** (its fingerprint must reproduce `export_sha256`; counts and per-split sizes must match it), requires internally-clean verifier records and an empty loader `problems`, cross-checks embedded vs standalone export records, and **pins the canonical evaluation recipe** (game, source split, role, `n_seqs=18`, full-set, tracker SHA, `imgsz=1536`, classes `0,1,2,3`, `conf=0.25`) for each evaluation independently. Suite 58 → **68**, with mutation checks confirming the poisoned-cache and dirty-verifier tests genuinely bite. Also fixed: the alias test now creates a **real** symlink; "sealed/one-shot/untouched" wording purged from `scripts/05,08,09`; `requirements.lock` labelled as local-macOS dev with the certified CUDA runtime recorded in `results/rung3_provenance/certified_runtime.json`. **State: the current checkpoint predates the cache defence, so `results/rung3_baseline.json` honestly reports `provably_leave_one_game_out: false` — 15/17 links pass, failing exactly `loader_labels_authenticated` and `loader_counts_match_inventory`. HOTA is unaffected (dev 0.6340 / final 0.6139, gates MET). One final retrain under the hardened guard is what closes it.** **Lesson: authenticate what the model *consumes*, not what sits on disk — the two diverge wherever a cache, a memo, or any derived artifact sits in between.**
