# pitch-vision — core dependencies (Phase 0–1). Requires Python 3.10+.
#
# Install PyTorch SEPARATELY for your platform first:
#   https://pytorch.org/get-started/locally/
#   macOS (Apple Silicon):   pip install torch torchvision
#   CUDA 12.x (Linux GPU):    pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124

ultralytics>=8.3.0      # YOLO detector + built-in ByteTrack / BoT-SORT tracking
lap>=0.5.12             # linear assignment — required by ByteTrack / BoT-SORT
opencv-python>=4.9
numpy>=1.24
pyyaml>=6.0
tqdm>=4.66

# Evaluation (Rung 1)
scipy>=1.11
git+https://github.com/JonathonLuiten/TrackEval.git             # HOTA / MOTA / IDF1

# Data (Rung 1)
SoccerNet>=0.1.60                                               # SoccerNet-Tracking downloader

# Promptable (Rung 2) — on non-CUDA machines build with SAM2_BUILD_CUDA=0
sam-2 @ git+https://github.com/facebookresearch/sam2.git

# --- Phase 3+ (uncomment when reached) ---
# boxmot>=11.0                                                     # standalone trackers + re-ID (BoT-SORT, OC-SORT, StrongSORT)
